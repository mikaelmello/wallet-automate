namespace WalletCore.Models {
    public enum AccountType {
        GENERAL,
        CASH,
        CURRENT_ACCOUNT,
        CREDIT_CARD,
        SAVING_ACCOUNT,
        BONUS,
        INSURANCE,
        INVESTMENT,
        LOAN,
        MORTGAGE,
        OVERDRAFT,
    }
}